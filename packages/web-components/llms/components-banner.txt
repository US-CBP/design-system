# Components/Banner

## Examples


### Banner

```tsx
({
  bannerTitle,
  bannerContent
}) => {
  return ` 
        <cbp-banner>
            <cbp-typography 
                slot='cbp-banner-title'
                tag='div'
                variant='heading-md'
                context='dark-always'
            >
                ${bannerTitle}
            </cbp-typography>
                ${bannerContent}
        </cbp-banner>
      `;
}
```