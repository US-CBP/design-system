# Content/Structured List

## Examples


### Structured List (Simple)

```tsx
({
  listItems,
  striped,
  selectable,
  showHeader,
  headerId,
  showFooter,
  listItemType,
  context,
  sx
}) => {
  setTimeout(() => {
    let anchors = document.querySelectorAll('cbp-structured-list a');
    anchors.forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
      });
    });
  }, 500);
  return ` 
    <cbp-structured-list
      ${striped ? `striped=${striped}` : ''}
      ${headerId ? `header-id="${headerId}"` : ``}
      ${context && context != 'light-inverts' ? `context="${context}"` : ''}   
      ${sx ? `sx='${JSON.stringify(sx)}'` : ''}
    >
      ${showHeader ? `<div slot="cbp-structured-list-header" ${headerId ? `id="${headerId}"` : ''}>${listItems.length} results, filters applied, etc. This acts as the "aria-description" for the list.</div>` : ''}
      ${listItemType == "structured list item" ? generateItems(listItems, selectable, context) : generateLIs(listItems, selectable, context)}
      ${showFooter ? `
      <div slot="cbp-structured-list-footer">
        <cbp-action-bar variant="inline" context="dark-inverts">
          <div slot="cbp-action-bar-info">0 items selected.</div>
          <cbp-button fill="ghost" context="dark-inverts" accessibility-text="Delete selected items">Delete</cbp-button>
          <cbp-button fill="ghost" context="dark-inverts" accessibility-text="Compare selected items">Compare</cbp-button>
        </cbp-action-bar>
      </div>` : ''}
    </cbp-structured-list>
  `;
}
```

### Structured List Collection

```tsx
({
  listItems,
  striped,
  selectable,
  showHeader,
  headerId,
  showFooter,
  listItemType,
  context,
  sx
}) => {
  setTimeout(() => {
    let anchors = document.querySelectorAll('cbp-structured-list a');
    anchors.forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
      });
    });
  }, 500);
  return ` 
    <cbp-structured-list
      ${striped ? `striped=${striped}` : ''}
      ${headerId ? `header-id="${headerId}"` : ``}
      ${context && context != 'light-inverts' ? `context="${context}"` : ''}   
      ${sx ? `sx='${JSON.stringify(sx)}'` : ''}
    >
      ${showHeader ? `<div slot="cbp-structured-list-header" ${headerId ? `id="${headerId}"` : ''}>${listItems.length} results, filters applied, etc. This acts as the "aria-description" for the list.</div>` : ''}
      ${listItemType == "structured list item" ? generateItems(listItems, selectable, context) : generateLIs(listItems, selectable, context)}
      ${showFooter ? `
      <div slot="cbp-structured-list-footer">
        <cbp-action-bar variant="inline" context="dark-inverts">
          <div slot="cbp-action-bar-info">0 items selected.</div>
          <cbp-button fill="ghost" context="dark-inverts" accessibility-text="Delete selected items">Delete</cbp-button>
          <cbp-button fill="ghost" context="dark-inverts" accessibility-text="Compare selected items">Compare</cbp-button>
        </cbp-action-bar>
      </div>` : ''}
    </cbp-structured-list>
  `;
}
```

### Structured List Media

```tsx
({
  listItems,
  striped,
  selectable,
  showHeader,
  headerId,
  showFooter,
  listItemType,
  context,
  sx
}) => {
  setTimeout(() => {
    let anchors = document.querySelectorAll('cbp-structured-list a');
    anchors.forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
      });
    });
  }, 500);
  return ` 
    <cbp-structured-list
      ${striped ? `striped=${striped}` : ''}
      ${headerId ? `header-id="${headerId}"` : ``}
      ${context && context != 'light-inverts' ? `context="${context}"` : ''}   
      ${sx ? `sx='${JSON.stringify(sx)}'` : ''}
    >
      ${showHeader ? `<div slot="cbp-structured-list-header" ${headerId ? `id="${headerId}"` : ''}>${listItems.length} results, filters applied, etc. This acts as the "aria-description" for the list.</div>` : ''}
      ${listItemType == "structured list item" ? generateItems(listItems, selectable, context) : generateLIs(listItems, selectable, context)}
      ${showFooter ? `
      <div slot="cbp-structured-list-footer">
        <cbp-action-bar variant="inline" context="dark-inverts">
          <div slot="cbp-action-bar-info">0 items selected.</div>
          <cbp-button fill="ghost" context="dark-inverts" accessibility-text="Delete selected items">Delete</cbp-button>
          <cbp-button fill="ghost" context="dark-inverts" accessibility-text="Compare selected items">Compare</cbp-button>
        </cbp-action-bar>
      </div>` : ''}
    </cbp-structured-list>
  `;
}
```

### Structured List Media Calendar

```tsx
({
  listItems,
  striped,
  selectable,
  showHeader,
  headerId,
  showFooter,
  listItemType,
  context,
  sx
}) => {
  setTimeout(() => {
    let anchors = document.querySelectorAll('cbp-structured-list a');
    anchors.forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
      });
    });
  }, 500);
  return ` 
    <cbp-structured-list
      ${striped ? `striped=${striped}` : ''}
      ${headerId ? `header-id="${headerId}"` : ``}
      ${context && context != 'light-inverts' ? `context="${context}"` : ''}   
      ${sx ? `sx='${JSON.stringify(sx)}'` : ''}
    >
      ${showHeader ? `<div slot="cbp-structured-list-header" ${headerId ? `id="${headerId}"` : ''}>${listItems.length} results, filters applied, etc. This acts as the "aria-description" for the list.</div>` : ''}
      ${listItemType == "structured list item" ? generateItems(listItems, selectable, context) : generateLIs(listItems, selectable, context)}
      ${showFooter ? `
      <div slot="cbp-structured-list-footer">
        <cbp-action-bar variant="inline" context="dark-inverts">
          <div slot="cbp-action-bar-info">0 items selected.</div>
          <cbp-button fill="ghost" context="dark-inverts" accessibility-text="Delete selected items">Delete</cbp-button>
          <cbp-button fill="ghost" context="dark-inverts" accessibility-text="Compare selected items">Compare</cbp-button>
        </cbp-action-bar>
      </div>` : ''}
    </cbp-structured-list>
  `;
}
```

### Structured List With Grid

```tsx
({
  listItems,
  striped,
  selectable,
  showHeader,
  headerId,
  showFooter,
  listItemType,
  context,
  sx
}) => {
  setTimeout(() => {
    let anchors = document.querySelectorAll('cbp-structured-list a');
    anchors.forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
      });
    });
  }, 500);
  return ` 
    <cbp-structured-list
      ${striped ? `striped=${striped}` : ''}
      ${headerId ? `header-id="${headerId}"` : ``}
      ${context && context != 'light-inverts' ? `context="${context}"` : ''}   
      ${sx ? `sx='${JSON.stringify(sx)}'` : ''}
    >
      ${showHeader ? `<div slot="cbp-structured-list-header" ${headerId ? `id="${headerId}"` : ''}>${listItems.length} results, filters applied, etc. This acts as the "aria-description" for the list.</div>` : ''}
      ${listItemType == "structured list item" ? generateItems(listItems, selectable, context) : generateLIs(listItems, selectable, context)}
      ${showFooter ? `
      <div slot="cbp-structured-list-footer">
        <cbp-action-bar variant="inline" context="dark-inverts">
          <div slot="cbp-action-bar-info">0 items selected.</div>
          <cbp-button fill="ghost" context="dark-inverts" accessibility-text="Delete selected items">Delete</cbp-button>
          <cbp-button fill="ghost" context="dark-inverts" accessibility-text="Compare selected items">Compare</cbp-button>
        </cbp-action-bar>
      </div>` : ''}
    </cbp-structured-list>
  `;
}
```