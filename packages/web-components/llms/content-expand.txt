# Content/Expand

## Examples


### Expand

```tsx
({
  label,
  content,
  open,
  headingLevel,
  context,
  sx
}) => {
  return ` 
    <cbp-expand
      ${label ? `label="${label}"` : ''}
      ${headingLevel ? `heading-level="${headingLevel}"` : ''}
      ${open ? 'open' : ''}
      ${context ? `context="${context}"` : ''}
      ${sx ? `sx='${JSON.stringify(sx)}'` : ''}
    >
      ${content}
    </cbp-expand>
  `;
}
```